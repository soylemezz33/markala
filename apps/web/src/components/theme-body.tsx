"use client";

/**
 * Tüm site light/beyaz tema. Dark surface kapatıldı.
 * Eskiden marketing rotalarında body'ye `surface-dark` class ekleniyordu —
 * artık eklenmiyor. Component placeholder olarak duruyor.
 */
export function ThemeBody() {
  return null;
}
